# PolymorphismAssignment
This C# console application demonstrates polymorphism using an interface.

- Defines an interface `IQuittable` with a `Quit()` method.
- Implements the interface in the `Employee` class, which also inherits from the abstract `Person` class.
- Uses polymorphism by creating an `IQuittable` object and calling `Quit()` on it.

